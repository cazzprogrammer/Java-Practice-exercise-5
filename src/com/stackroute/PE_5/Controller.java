package com.stackroute.PE_5;

public class Controller {

    public static void main(String args[])
    {
        //UpdateArrayList updateArrayList = new UpdateArrayList();
        //updateArrayList.updateArrayList();

        //WordCount wordCount = new WordCount();
        //wordCount.wordCountInSentence("one one -one___two,,three,one @three*one?two");
        Wordbool wordbool = new Wordbool();
        String arr[] = {"a","b","c","c"};
        wordbool.wordBoolFrequency(arr);
    }
}
